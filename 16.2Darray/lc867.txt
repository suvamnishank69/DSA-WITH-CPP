// leetcode 867q



class solution{
    vector<vector<int>> transpose(vector<vector<int>>& martix) {

        int n = matrix.size(); //==> give the size of row (work only for vector)
        int m = matrix[0].size();  // give the size og col (work only for vector)

        vector<vector<int>>ans(m,vector<int>(n));

        for(int i=0;i<n;i++){
            for(int j=0;j<m;j++){
                ans[j][i]=matrix[i][j];
            }
        }

        return ans;


    }
};